services:
  mysql:
    image: mysql:8
    environment:
      MYSQL_DATABASE: cpudb
      MYSQL_USER: cpuuser
      MYSQL_PASSWORD: cpupass
      MYSQL_ROOT_PASSWORD: rootpass
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
